import { View, Text } from "react-native";

export enum Side {
  LEFT,
  RIGHT,
  NONE,
}

const Wave = () => {
  return (
    <View>
      <Text>Wave</Text>
    </View>
  );
};

export default Wave;
